# Helix experiment — $experiment_id

This PR is opened by the **Helix Architect** agent. It composes a
pre-registered multivariate experiment behind a single PostHog feature flag.
Merging it does not ship anything to users by default — rollout is controlled
end-to-end via the flag.

## Why this experiment

We are testing several variants against control to move our primary KPI:

- **Primary KPI:** `$primary_kpi`
- **Guardrail KPIs:** $guardrail_kpis

## Variants

$variants_md

## Traffic split

$traffic_split

## Decision rule

> $decision_rule

## Sampling plan

- **Min N per arm:** $min_n_per_arm
- **Observation window:** $min_observation_days–$max_observation_days days
- **Registered at:** $registered_at

## Multivariate flag

- **Provider:** PostHog (multivariate)
- **Flag key:** `$flag_key`
- **Initial rollout:** $flag_rollout_pct% of eligible users
- **Default (off-treatment):** `control`

## Files in this branch

$files_md

---

> Auto-generated by the Helix Architect agent. Do not edit the flag config or
> the variant files manually while the experiment is `running` — the Witness
> agent reads the flag and these files as the source of truth.
